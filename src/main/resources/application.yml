server:
  port: 5050

spring:
  datasource:
    url: jdbc:h2:file:./Desktop/Major Project/h2_database/iesardb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
